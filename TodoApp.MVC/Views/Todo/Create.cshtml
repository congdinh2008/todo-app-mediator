@model TodoViewModel
@{
    ViewData["Title"] = "Home Page";
}

<div class="card">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h1>Create Todo</h1>
    </div>
    <form action="@Url.Action("Create", "Todo")" method="post">
        <div class="card-body">
            <div class="mb-3">
                <label for="name" class="form-label">Title</label>
                <input type="text" class="form-control" asp-for="Title" id="name" placeholder="Todo Title">
                <span asp-validation-for="Title" class="text-danger"></span>
            </div>
            <div class="mb-3">
                <label for="name" class="form-label">Category</label>
                <select class="form-select" asp-for="CategoryId" id="category" asp-items="@ViewBag.Categories"></select>
                <span asp-validation-for="Title" class="text-danger"></span>
            </div>
            <div class="form-check">
                <input class="form-check-input" type="checkbox" id="is-completed" asp-for="IsCompleted">
                <label class="form-check-label" for="is-completed">
                    Is Completed?
                </label>
                <span asp-validation-for="IsCompleted" class="text-danger"></span>
            </div>
        </div>
        <div class="card-footer d-flex justify-content-end">
            <button class="btn btn-primary" type="submit">
                <i class="fa-solid fa-save"></i> Create
            </button>
        </div>
    </form>
</div>